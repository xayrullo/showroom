export const STORAGE_NAME = {
  LOCALE: "locale",
  VERSION: "version",
};

export const LOCALES = ["uz", "en", "ru"];

export type LanguageCode = "uz" | "en" | "ru";

export const IMAGE_URL = import.meta.env.VITE_BASE_URL + "/";
